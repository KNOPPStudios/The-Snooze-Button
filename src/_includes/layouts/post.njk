---
layout: layouts/base.njk
---
<article>
  <h1>{{ title }}</h1>
  <p><small>{{ date | date("MMMM d, yyyy") }}{% if tags and tags.length %} · {{ tags | join(", ") }}{% endif %}</small></p>
  {{ content | safe }}
</article>

{% set rel = page | related(collections.posts, 3) %}
{% include "components/related.njk" %}
